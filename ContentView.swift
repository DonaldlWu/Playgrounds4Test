import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Test")
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, world!")
        }
    }
}
